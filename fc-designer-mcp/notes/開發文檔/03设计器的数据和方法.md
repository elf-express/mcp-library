# 设计器的数据和方法

> Source: https://pro.form-create.com/doc/methods

---

# 設計器的資料和方法 ​

透過 `refs.designer` 可以呼叫 `fc-designer` 元件的各種方法來操作和配置設計器。以下是各方法的詳細說明及示例：

> 移動端設計器是`fc-designer-mobile`元件

**Vue3**

vue

```
<template>
    <fc-designer ref="designer"/>
</template>
<script setup>
    import {onMounted} from "vue";
    const designer = ref(null);

    //注意：需等待组件完成初始化后，方可调用其方法
    onMounted(() => {
        designer.value.addComponent([
            //扩展组件
        ])
    });
</script>
```

**Vue2**vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
</template>
<script>
    export default {
        name: 'Component',
        //注意：需等待组件完成初始化后，方可调用其方法
        mounted() {
            this.$refs.designer.addComponent([
                //扩展组件
            ])
        }
    };
</script>
```

## 欄位 ​

元件例項的欄位以及說明：

| 欄位名 | 描述 |
| --- | --- |
| activeRule | 當前選中的元件規則。 |
| formOptions | 當前表單的規則。 |

## 方法 ​

元件例項方法及使用說明：

| 方法名 | 描述 | 型別定義 |
| --- | --- | --- |
| getConfig | 獲取設計器的配置項。 | (key: string, def: any) => any |
| addComponent | 將一個或多個元件模板或拖拽元件新增到設計器，並根據 menu 欄位自動歸類到對應選單下。檢視擴展元件。 | AddComponent |
| setMenuItem | 將拖拽元件覆蓋新增到指定的選單下，menuName 為目標選單名稱。檢視擴展元件分組。 | (menuName: string, list: MenuList) => void |
| addMenu | 向設計器左側選單欄新增一個新的選單項。檢視擴展元件分組。 | (menu: Menu) => void |
| removeMenu | 移除指定的選單項。 | (name: string) => void |
| appendMenuItem | 向指定選單新增選單項。 | (name: string, item: MenuItem \| MenuItem[]) => void |
| removeMenuItem | 從指定選單移除選單項。 | (item: string \| MenuItem) => void |
| setRule | 設定表單生成的規則，支援字串或規則物件陣列。檢視儲存JSON規則。 | (rule: string \| Rule[], partFlag?: boolean) => void |
| setOption | 設定表單的配置選項（如佈局、標籤寬度）。檢視修改表單預設配置。別名方法：setOptions。 | (opt: Options) => void |
| mergeOptions | 透過合併方式更新表單配置選項。檢視修改表單預設配置。 | (opt: Options) => void |
| getRule | 獲取當前表單的渲染規則（陣列形式）。 | () => Rule[] |
| getJson | 獲取當前表單的 JSON 渲染規則（字串形式）。 | () => string |
| getOptionsJson | 獲取當前表單的 JSON 配置（字串形式）。 | () => string |
| getHTML | 獲取表單渲染的 HTML。 | () => string |
| getTemplate | 獲取表單渲染的 Vue 模板。 | () => string |
| getSFC | 獲取表單渲染的 SFC 模板。 | () => string |
| getFormData | 獲取當前表單的 formData 物件。檢視預設值。 | () => Object |
| setFormData | 設定表單的 formData 物件（用於預填充資料）。檢視預設值。 | (formData: Object) => void |
| getDescription | 獲取表單的層級結構資料。檢視獲取層級結構。 | () => TreeData |
| getFormDescription | 獲取表單元件的層級結構資料。檢視獲取層級結構。 | () => TreeData |
| openPreview | 開啟表單預覽模式。 | () => void |
| openInputData | 開啟或關閉資料錄入模式（true 開啟，false 關閉）。檢視預設值。 | (open: boolean) => void |
| clearDragRule | 清空設計器中的所有表單元件。 | () => void |
| fields | 獲取設計器中所有欄位的名稱列表。檢視管理欄位列表。 | () => string[] |
| triggerActive | 選中指定元件（支援規則物件或 field/name/_fc_id）。 | (rule: Rule \| string) => void |
| clearActiveRule | 清除當前選中的元件狀態。 | () => void |
| setFormRuleConfig | 設定表單配置規則（同 config.formRule）。檢視擴展配置規則。 | (rule: () => Rule[], append: boolean) => void |
| setBaseRuleConfig | 設定元件基礎配置規則（同 config.baseRule）。檢視擴展配置規則。 | (rule: () => Rule[], append: boolean) => void |
| setComponentRuleConfig | 設定指定元件的屬性配置規則（同 config.componentRule）。檢視擴展配置規則。 | (id: string, rule: () => Rule[], append: boolean) => void |
| setGlobalData | 預設全域資料來源（用於設計器）。檢視預定義全域資料。 | (data: GlobalData) => void |
| setGlobalEvent | 預設全域事件（用於設計器）。檢視預定義全域資料。 | (event: GlobalEvent) => void |
| setGlobalClass | 預設全域樣式（用於設計器）。檢視預定義全域資料。 | (class: GlobalClass) => void |
| setGlobalVariable | 預設全域變數（用於設計器）。檢視預定義全域資料。 | (variable: GlobalVariable) => void |
| openGlobalEventDialog | 開啟全域事件彈窗。 | () => void |
| openGlobalFetchDialog | 開啟全域資料來源彈窗。 | () => void |
| openGlobalClassDialog | 開啟全域樣式彈窗。 | () => void |
| openGlobalVariableDialog | 開啟全域變數彈窗。 | () => void |
| setDevice | 設定設計器的裝置型別。 | (device: string) => void |

## 示例 ​

### 擴展自定義元件 ​

自定義元件允許開發者根據業務需求擴展表單設計器的功能，例如新增特定的輸入控制元件、業務模組或第三方UI元件。

vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
</template>
<script>
    import lineChart from './rule/charts/lineChart';
    import areaChart from './rule/charts/areaChart';
    import barChart from './rule/charts/barChart';
    import stripeChart from './rule/charts/stripeChart';
    import pieChart from './rule/charts/pieChart';
    import funnelChart from './rule/charts/funnelChart';
    import gaugeChart from './rule/charts/gaugeChart';
    import radarChart from './rule/charts/radarChart';
    import scatterChart from './rule/charts/scatterChart';
    import customChart from './rule/charts/customChart';

    export default {
        name: 'Component',
        //注意：需等待组件完成初始化后，方可调用其方法
        mounted() {
            this.$refs.designer.addComponent([
                lineChart, areaChart, barChart, stripeChart, pieChart, 
                funnelChart, gaugeChart, radarChart, scatterChart, customChart
            ])
        }
    };
</script>
```

### 匯入設計表單規則 ​

在設計器中匯入預先設計好的表單規則，可以二次編輯已有配置

vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
</template>
<script>
    import formCreate from '@form-create/element-ui';
    export default {
        name: 'Component',
        //注意：需等待组件完成初始化后，方可调用其方法
        mounted() {
            axios.get('/api/getForm').then(({ ruleJson, optionsJson }) => {
                this.$refs.designer.setRule(formCreate.parseJson(ruleJson));
                this.$refs.designer.setOptions(formCreate.parseJson(optionsJson));
            })
        }
    };
</script>
```

### 儲存設計表單規則 ​

在設計器中完成表單設計後，可透過以下步驟儲存規則：

vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
    <el-button @click="save">保存</el-button>
</template>
<script>
    import formCreate from '@form-create/element-ui';
    export default {
        name: 'Component',
        methods: {
            save() {
                axios.post('/api/saveForm', {
                    rules: formCreate.toJson(this.$refs.designer.getRule()), //JSON字符串
                    options: formCreate.toJson(this.$refs.designer.getOptions()), //JSON字符串
                }).then(response => {
                    // 处理保存成功的逻辑
                    console.log('保存成功');
                })
            },
        }
    };
</script>
```

### 手動選中指定元件 ​

在表單設計器初始化完成後，透過 triggerActive 方法並傳入欄位名、元件名或規則物件，來手動選中並啟用指定的表單元件。

vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
</template>
<script>
    import formCreate from '@form-create/element-ui';
    export default {
        name: 'Component',
        //注意：需等待组件完成初始化后，方可调用其方法
        mounted() {
            //通过组件 field 选中
            this.$refs.designer.triggerActive('field');
            //通过组件 name 选中
            this.$refs.designer.triggerActive('name');
            //通过组件规则选中
            this.$refs.designer.triggerActive(rule);
        }
    };
</script>
```

### 選單管理 ​

動態管理設計器的選單項，包括新增、刪除、修改選單。

vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
    <el-button @click="addCustomMenu">添加自定义菜单</el-button>
    <el-button @click="removeMenu">移除菜单</el-button>
</template>
<script>
    export default {
        name: 'Component',
        mounted() {
            // 添加自定义菜单
            this.$refs.designer.addMenu({
                name: 'custom',
                title: '自定义组件',
                list: [
                    {
                        name: 'custom-input',
                        title: '自定义输入框',
                        // 组件配置...
                    }
                ]
            });
        },
        methods: {
            addCustomMenu() {
                // 向现有菜单添加组件
                this.$refs.designer.appendMenuItem('custom', {
                    name: 'custom-button',
                    title: '自定义按钮'
                });
            },
            removeMenu() {
                // 移除菜单项
                this.$refs.designer.removeMenuItem('custom-button');
                // 或移除整个菜单
                this.$refs.designer.removeMenu('custom');
            }
        }
    };
</script>
```

### 裝置型別切換 ​

動態切換設計器的裝置型別，支援PC和移動端預覽。

vue

```
<template>
    <fc-designer ref="designer"></fc-designer>
    <el-button @click="switchToMobile">切换到移动端</el-button>
    <el-button @click="switchToPC">切换到PC端</el-button>
</template>
<script>
    export default {
        name: 'Component',
        methods: {
            switchToMobile() {
                // 切换到移动端
                this.$refs.designer.setDevice('mobile');
            },
            switchToPC() {
                // 切换到PC端
                this.$refs.designer.setDevice('pc');
            }
        }
    };
</script>
```

## 型別定義 ​

**展開**ts

```
// 单组件或组件数组添加
type AddComponent =
    (dragRule: dragRule | dragTemplateRule) => void |
        (dragRule: Array<dragRule | dragTemplateRule>) => void;

        // 菜单项操作
type SetMenuItem = (menuName: string, list: MenuList) => void;
type AddMenu = (menu: Menu) => void;

// 规则与配置操作
type SetRule = (rule: string | Rule[]) => void;
type SetOption = (opt: Options) => void;
type MergeOptions = (opt: Options) => void;
type GetRule = () => Rule[];
type GetJson = () => string;
type GetOption = () => Options;
type GetOptionsJson = () => string;
type GetHTML = () => string;
type GetTemplate = () => string;
type GetSFC = () => string;

// 表单数据操作
type GetFormData = () => Object;
type SetFormData = (formData: Object) => void;

// 结构数据获取
type GetDescription = () => TreeData;
type GetFormDescription = () => TreeData;

// 视图控制
type OpenPreview = () => void;
type OpenInputData = (open: boolean) => void;

// 设计器操作
type ClearDragRule = () => void;
type Fields = () => string[];
type TriggerActive = (rule: Rule | string) => void;
type ClearActiveRule = () => void;

// 规则配置
type SetFormRuleConfig = (rule: () => Rule[], append: boolean) => void;
type SetBaseRuleConfig = (rule: () => Rule[], append: boolean) => void;
type SetValidateRuleConfig = (rule: () => Rule[], append: boolean) => void;
type SetComponentRuleConfig = (id: string, rule: () => Rule[], append: boolean) => void;

// 全局预设
type SetGlobalData = (data: GlobalData) => void;
type SetGlobalEvent = (event: GlobalEvent) => void;
type SetGlobalClass = (class: GlobalClass) => void;
type SetGlobalVariable = (variable: GlobalVariable) => void;

// 弹窗控制
type OpenGlobalEventDialog = () => void;
type OpenGlobalFetchDialog = () => void;
type OpenGlobalClassDialog = () => void;
type OpenGlobalVariableDialog = () => void;

// 设备控制
type SetDevice = (device: string) => void;

// 菜单管理
type RemoveMenu = (name: string) => void;
type AppendMenuItem = (name: string, item: MenuItem | MenuItem[]) => void;
type RemoveMenuItem = (item: string | MenuItem) => void;

// 配置获取
type GetConfig = (key: string, def: any) => any;
```

**完整配置項的型別定義可參考 TypeScript 資料結構文件：[TS型別定義](https://pro.form-create.com/doc/ts)**

## 注意事項 ​

### 元件初始化時機 ​

**重要**：所有方法呼叫必須在元件完成初始化後進行，否則會報錯。

vue

```
<script>
export default {
    mounted() {
        // ✅ 正确：在 mounted 生命周期中调用
        this.$refs.designer.addComponent([...]);
    },
    methods: {
        wrongWay() {
            // ❌ 错误：在组件未挂载时调用
            this.$refs.designer.setRule([]);
        }
    }
};
</script>
```

### 非同步操作處理 ​

某些方法涉及非同步操作，需要正確處理：

vue

```
<script>
export default {
    async mounted() {
        // ✅ 正确：等待异步操作完成
        await this.loadFormData();
        this.$refs.designer.setRule(this.rules);
    },
    methods: {
        async loadFormData() {
            const response = await fetch('/api/form-data');
            return response.json();
        }
    }
};
</script>
```