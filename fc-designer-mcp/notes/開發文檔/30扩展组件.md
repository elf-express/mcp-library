# 扩展组件

> Source: https://pro.form-create.com/doc/component

---

# 擴展元件 ​

自定義元件可以被整合到設計器中，用於擴展功能。以下是如何建立和使用自定義元件的詳細說明。

在[渲染器文件](https://form-create.com/v3/element-ui/components/input)中可以檢視內建表單元件及其所有可配置引數。

在[自定義表單元件](https://form-create.com/v3/guide/custom-form-component)可查閱自定義表單元件的開發規範與標準要求。

在[原始碼](https://gitee.com/xaboy/form-create-designer/tree/next/packages/element-ui/src/config/rule)中可以檢視更多元件拖拽配置規則，供您開發參考。

注意

現已支援透過 **FcDesigner.addDragRule** 方法全域註冊拖拽規則，實現一次配置多處使用。[方法說明](https://pro.form-create.com/doc/proto)

## 擴展元件教程 ​

手把手教你建立一個 Title 標題元件，可在表單中靈活顯示各類標題文字。

![component1](https://pro.form-create.com/doc/img/component1.png)

### 1. 封裝元件 ​

新建 Title.vue 檔案，開發標題元件的功能樣式。

vue

```
<template>
    <div class="_fc-title" :class="size || 'h2'" :style="textStyle">
        {{ title }}
    </div>
</template>
<script>
    import {defineComponent} from 'vue';
    export default defineComponent({
        name: 'FcTitle',
        data() {
            return {};
        },
        props: {
            title: String,
            size: String,
            align: String,
        },
        computed: {
            textStyle() {
                return {
                    textAlign: this.align || 'left',
                }
            }
        }
    });
</script>
<style>
    ._fc-title {
        width: 100%;
        font-size: 16px;
        font-weight: 600;
        margin-top: 1em;
        margin-bottom: 16px;
    }

    ._fc-title.h1, ._fc-title.h2 {
        padding-bottom: .3em;
        border-bottom: 1px solid #eee
    }

    ._fc-title.h1 {
        font-size: 32px;
        line-height: 1.2
    }

    ._fc-title.h2 {
        font-size: 24px;
        line-height: 1.225
    }

    ._fc-title.h3 {
        font-size: 20px;
        line-height: 1.43
    }

    ._fc-title.h4 {
        font-size: 16px;
    }

    ._fc-title.h5 {
        font-size: 14px;
    }

    ._fc-title.h6 {
        font-size: 12px;
    }
</style>
```

### 2. 掛載元件 ​

在 main.js 中掛載 Title 元件

ts

```
import Title from 'Title.vue'
import FcDesigner from '/path/to/fcDesignerPro/dist/index.es.js'

FcDesigner.component('Title', Title);
//或者通过 app 全局挂载
app.component('Title', Title);
```

或者在元件中掛載 Title 元件

vue

```
<script>
    import {defineComponent} from 'vue'; 
    import Title from 'Title.vue';
    import FcDesigner from '/path/to/fcDesignerPro/dist/index.es.js'
    export default defineComponent({
        mounted() {
            FcDesigner.component('Title', Title);
        }
    });
</script>
```

### 3.定義拖拽規則 ​

定義 Title 元件的拖拽規則

js

```
const titleDragRule = {
    menu: 'aide',
    icon: 'icon-title',
    label: '标题',
    //唯一 ID
    name: 'title',
    //组件可以配置的事件
    event: ['click'],
    rule() {
        //组件的渲染规则
        return {
            //组件的名称, 与上一步是对应
            type: 'Title',
            props: {
                title: '自定义标题',
            },
        };
    },
    props() {
        //组件右侧的配置项,与组件中的 props 对应
        return [
            {type: 'input', field: 'title', title: '标题'},
            {
                type: 'select',
                field: 'size',
                title: '尺寸',
                //默认值
                value: 'H2',
                options: [1, 2, 3, 4, 5, 6].map(k => {
                    return {label: 'H' + k, value: 'h' + k};
                })
            }, {
                type: 'select',
                field: 'align',
                title: '位置',
                options: [
                    {label: '左侧', value: 'left'},
                    {label: '居中', value: 'center'},
                    {label: '右侧', value: 'right'}
                ]
        }];
    }
};
```

**在[渲染器文件](https://form-create.com/v3/element-ui/components/input)中可以檢視內建表單元件及其所有可配置引數。**

![component2](https://pro.form-create.com/doc/img/component2.png)

根據 props 函式定義的規則，自動生成元件右側的配置面板選項。

### 4.匯入拖拽規則 ​

定義好拖拽規則後，需要將其掛載到設計器中。

js

```
// 假设这是你的 Vue 组件
export default {
    mounted() {
        // 挂载拖拽规则
        this.$refs.designer.addComponent(titleDragRule);
    }
};
```

透過以上步驟，您可以將自定義元件與設計器整合，併為其定義拖拽行為和屬性配置，使其在設計器中成為可用的元件。這不僅提高了設計器的靈活性，還可以根據具體需求擴展其功能。

## 擴展表單元件教程 ​

自定義表單元件可以用來實現業務特定的表單控制元件。下面將介紹如何定義和使用自定義表單元件，包括如何匯入、掛載元件及設定其拖拽規則。

**在[自定義表單元件](https://form-create.com/v3/guide/custom-form-component)可查閱自定義表單元件的開發規範與標準要求。**

### 1. 封裝元件 ​

手把手教你基於 Element Plus 的 Input 元件，開發定製化的新輸入框元件。

新建 Input.vue 檔案，開發輸入框元件的功能。

vue

```
<template>
    <el-input :disabled="disabled" :modelValue="modelValue" @update:modelValue="handleChange" v-bind="$attrs">
        <!--自定义组件的插槽-->
        <template #prepend>Http://</template>
    </el-input>
</template>
<script>
    import {defineComponent} from 'vue';
    export default defineComponent({
        name: 'FcInput',
        data() {
            return {};
        },
        emits: ['update:modelValue', 'change'],
        props: {
            //获取表单数据
            modelValue: String,
            //支持表单禁用功能
            disabled: Boolean
        },
        methods: {
            //更新表单数据
            handleChange(val) {
                this.$emit('update:modelValue', val);
                //触发 change 事件
                this.$emit('change', val);
            }
        }
    });
</script>
```

**使用 `v-bind="$attrs"` 可自動透傳所有配置項到 el-input 元件，無需重複定義即可繼承全部屬性。**

### 2. 掛載元件 ​

在 main.js 中掛載 Input 元件

ts

```
import Input from 'Input.vue'
import FcDesigner from '/path/to/fcDesignerPro/dist/index.es.js'

FcDesigner.component('FcInput', Input);
//或者通过 app 全局挂载
app.component('FcInput', Input);
```

或者在元件中掛載 Title 元件

vue

```
<script>
    import {defineComponent} from 'vue'; 
    import Input from 'Input.vue';
    import FcDesigner from '/path/to/fcDesignerPro/dist/index.es.js'
    export default defineComponent({
        mounted() {
            FcDesigner.component('FcTitle', Input);
        }
    });
</script>
```

### 3.定義拖拽規則 ​

定義 Input 元件的拖拽規則

ts

```
import uniqueId from '@form-create/utils/lib/unique';
const InputDragRule = {
    menu: 'main',
    icon: 'icon-input',
    label: '自定义输入框',
    //唯一 ID
    name: 'FcInput',
    //表单组件标识
    input: true,
    //组件的事件
    event: ['blur', 'focus', 'change', 'input', 'clear'],
    validate: ['string', 'url', 'email'],
    rule({t}) {
        return {
            //组件的名称, 与上一步是对应
            type: 'FcInput',
            field: uniqueId(),
            title: '自定义输入框',
            info: '',
            $required: false,
            props: {}
        };
    },
    props() {
        return [
            {
                type: 'switch',
                title: '禁用',
                field: 'disabled'
            },
            {
                type: 'switch',
                title: '清空',
                field: 'clearable'
            },
            {
                type: 'input',
                title: '占位文字',
                field: 'placeholder'
            },
        ];
    }
};
export default InputDragRule;
```

**在[渲染器文件](https://form-create.com/v3/element-ui/components/input)中可以檢視內建表單元件及其所有可配置引數。**

**注意:** 呼叫 `uniqueId` 方法需先安裝 `@form-create/utils` 包，或自定義實現以字母開頭的唯一ID生成邏輯（禁止包含符號）。

sh

```
npm install @form-create/utils@^3
```

### 4.匯入拖拽規則 ​

定義好拖拽規則後，需要將其掛載到設計器中。

js

```
// 假设这是你的 Vue 组件
export default {
    mounted() {
        // 挂载拖拽规则
        this.$refs.designer.addComponent(InputDragRule);
    }
};
```

## `props` 配置名稱對映規則 ​

在元件配置規則(props)方法中，如果 `field` 以 `formCreate` 開頭，系統將自動修改規則中對應的欄位。這使得開發者可以透過統一的方式來定義和對映元件的配置。

| 欄位 | 說明 |
| --- | --- |
| type | 修改rule.props.type欄位 |
| options>label | 修改rule.props.options.label欄位 |
| formCreateStyle | 修改rule.style欄位 |
| formCreateStyle>width | 修改rule.style.width欄位 |
| formCreateChild | 修改rule.children[0] |

例如:

js

```
{
    props() {
        return [
            {
                type: 'switch',
                title: '禁用',
                //配置名称, 修改 rule.props.disabled
                field: 'disabled'
            },
            {
                type: 'switch',
                title: '清空',
                //配置名称, 修改 rule.props.clearable
                field: 'clearable'
            },
            {
                type: 'input',
                title: '占位文字',
                //配置名称, 修改 rule.props.placeholder
                field: 'placeholder'
            },
        ];
    }
}
```

## 更多示例 ​

### 限制只能拖入1個 ​

ts

```
const InputDragRule = {
    menu: 'main',
    icon: 'icon-input',
    label: '自定义输入框',
    name: 'FcInput',
    //只能拖入1个
    only: true,
    //...
}
```

### 限制只能拖入到 group 元件中 ​

ts

```
const InputDragRule = {
    menu: 'main',
    icon: 'icon-input',
    label: '自定义输入框',
    name: 'FcInput',
    //限制只能拖入到 group 组件中
    allowDragTo: ['group'],
    //...
}
```

### 行內元件 ​

行內元件的寬度不會自動撐開`100%`

ts

```
const InputDragRule = {
    menu: 'main',
    icon: 'icon-input',
    label: '自定义输入框',
    name: 'FcInput',
    //行内组件标识
    inline: true,
    //...
}
```

### 定義元件需要用到的多語言ID ​

ts

```
const InputDragRule = {
    menu: 'subform',
    icon: 'icon-table-form',
    label: '表格表单',
    name: 'tableForm',
    input: true,
    mask: false,
    subForm: 'array',
    //多语言 ID
    languageKey: ['add', 'operation', 'dataEmpty'],
    //...
}
```

### 禁止配置元件樣式 ​

ts

```
const InputDragRule = {
    menu: 'main',
    icon: 'icon-input',
    label: '自定义输入框',
    name: 'FcInput',
    //禁止配置组件样式
    style: false,
    //...
}
```

### 轉換元件規則 ​

例如 `group` 元件的子表單規則實際儲存在 `props.rule` 中，但為便於視覺化設計會展示在 `children` 欄位，這個時候就需要進行雙向資料轉換適配。

ts

```
const InputDragRule = {
    menu: 'main',
    icon: 'icon-input',
    label: '自定义输入框',
    name: 'FcInput',
    //加载设计器渲染规则时
    loadRule(rule) {
        rule.children = rule.props.rule || [];
        rule.type = 'FcRow';
        delete rule.props.rule;
    },
    //导出最终渲染规则时触发
    parseRule(rule) {
        rule.props.rule = rule.children;
        rule.type = 'group';
        delete rule.children;
        delete rule.props.mode;
    },
    //...
}
```

## 資料結構 ​

ts

```
interface DragRule {
    //组件id,不能重复
    name: string;
    //组件的名称
    label: string;
    //组件的图标
    icon: string;
    //插入的分类
    menu?: MenuName;
    //是否支持样式配置
    style?: boolean;
    //是否是行内组件
    inline?: boolean;
    //是否是表单组件
    input?: boolean;
    //控制最多拖入几个子组件
    maxChildren?: number;
    //如果是子表单组件,需要定义`value`的类型
    subForm?: 'object' | 'array';
    //可拖入的组件列表(容器组件可用)
    allowDrag?: AllowDrag;
    //不可拖入的组件列表(容器组件可用)
    denyDrag?: DenyDrag;
    //允许拖入到那些组件中
    allowDragTo?: string | string[];
    //组件,不建议使用
    component?: Component;
    //渲染子组件的列表
    subRender: ({h, resolveComponent, subRule, rule}: {
        h: typeof H<any>,
        resolveComponent: typeof ResolveComponent,
        subRule: Rule,
        rule: Rule
    }) => Array<{
        label: string;
        vnode: VNode
    }>;
    //配置组件条件选择方式
    condition: conditionComponentType | ((rule: Rule) => ({
        //组件类型
        type: conditionComponentType;
        //选择项
        options?: any[];
        //组件类型
        props?: Object;
    }));
    //多语言配置项
    languageKey: string[];
    //表单全局配置
    formOptions?: {
        globalClass?: GlobalClass;
        globalVariable?: GlobalVariable;
        globalData?: GlobalData;
        globalEvent?: GlobalEvent;
        language?: Object;
        style?: string;
    } | string;
    //组件的生成规则
    rule(arg: { t: t }): Rule;
    //组件属性配置的规则
    props(rule: Rule, arg: { t: t, api: Api }): Rule[];
    //导出规则时通过这个方法转成最终规则
    parseRule?: (rule: Rule) => void;
    //导入规则时通过这个方法转成设计器中的渲染规则
    loadRule?: (rule: Rule) => void;
    //当props中的字段变化时触发
    watch?: {
        [key: string]: (arg: { value: any, rule: Rule, api: Api, field: string }) => void;
    };
    //是否有配套的子组件,例如Row和Col
    children?: string;
    //初始化时渲染几个子组件
    childrenLen?: number;
    //当前组件的操作容器是否显示在组件内部,为false时操作容器包裹当前组件
    inside?: true | boolean;
    //是否可以拖入其他组件到当前组件内部
    drag?: true | string | boolean;
    //是否显示拖拽按钮
    dragBtn?: false | boolean;
    //控制操作操作按钮是否显示,显示哪些
    handleBtn?: true | boolean | Array<'create' | 'copy' | 'addChild' | 'delete'>;
    //隐藏基础配置中的字段
    hiddenBaseField?: string[];
    //是否显示遮罩, 避免对组件操作. 建议有子组件时为true,其他为false
    mask?: false | boolean;
    //是否只能拖入一个
    only?: boolean;
    //是否生成name
    aide?: boolean;
    //当前组件的插槽
    easySlots?: Array<string| {
        //插槽名称
        value: string;
        //插槽简介
        label?: string;
        //类型
        type?: 'icon' | 'text'
    }>;
    //当前组件支持的事件
    event?: string[];
    //当前组件`value`的数据类型, required 时只显示是否必填
    validate?: string[] | 'required' | boolean;
}
```