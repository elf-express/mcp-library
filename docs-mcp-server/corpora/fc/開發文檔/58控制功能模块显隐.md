# 控制功能模块显隐

> Source: https://pro.form-create.com/doc/hidden-module

---

# 控制功能模組顯隱 ​

透過配置config物件，您可以控制設計器中的功能模組的顯示與隱藏。這些配置可以幫助您根據業務需求定製設計器的介面和功能。

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        //隐藏拖拽操作按钮
        hiddenDragMenu: false;
        //隐藏拖拽按钮
        hiddenDragBtn: false;
        //是否显示左侧工具栏
        showMenuBar?: true;
        //是否显示快速排序按钮
        showQuickLayout?: true;
        //是否显示辅助线按钮
        showGridLine?: true;
        //是否显示预览按钮
        showPreviewBtn?: true;
        //是否显示打印按钮
        showPrintBtn?: true;
        //是否显示保存按钮
        showSaveBtn?: false;
        //是否显示保存字段按钮
        showSaveFieldBtn?: true;
        //是否显示导入Excel按钮
        showImportBtn?: true;
        //是否显示右侧的配置界面
        showConfig?: true;
        //是否显示组件的基础配置表单
        showBaseForm?: true;
        //是否显示组件的编号
        showComponentName?: true;
        //是否显示组件联动
        showControl?: true;
        //是否显示绑定变量
        showVariable?: true;
        //是否显示模块管理
        showPageManage?: true;
        //是否显示组件的高级配置表单
        showAdvancedForm?: true;
        //是否显示组件的属性配置表单
        showPropsForm?: true;
        //是否显示组件的样式配置表单
        showStyleForm?: true;
        //是否显示组件的事件配置表单
        showEventForm?: true;
        //是否显示组件的验证配置表单
        showValidateForm?: true;
        //是否显示json预览按钮
        showJsonPreview?: true;
        //是否显示自定义props按钮
        showCustomProps?: true;
        //是否显示多语言配置
        showLanguage?: true;
        //是否显示标签编辑
        showLabelConfig?: true;
        //是否显示表单配置
        showFormConfig?: true;
        //是否显示左侧的模板列表
        showTemplate?: true;
        //是否显示多端适配选项
        showDevice?: true;
        //控制组件验证是否只显示必填验证
        validateOnlyRequired?: true;
    }
</script>
```

## 隱藏內建元件 ​

[隱藏內建元件](https://pro.form-create.com/doc/hidden-item)

## 關閉確認彈窗 ​

![exitConfirm](https://pro.form-create.com/doc/img/exitConfirm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        exitConfirm: false // 关闭确认弹窗
    }
</script>
```

## 隱藏左側工具欄 ​

![showMenuBar](https://pro.form-create.com/doc/img/hidden1.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showMenuBar: false // 隐藏左侧工具栏
    }
</script>
```

## 隱藏快速排序按鈕 ​

![showQuickLayout](https://pro.form-create.com/doc/img/hidden2.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showQuickLayout: false // 隐藏快速排序按钮
    }
</script>
```

## 隱藏輔助線按鈕 ​

![showGridLine](https://pro.form-create.com/doc/img/hidden6.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showGridLine: false // 隐藏辅助线按钮
    }
</script>
```

## 隱藏列印表單按鈕 ​

![showPrintBtn](https://pro.form-create.com/doc/img/hidden7.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showPrintBtn: false // 隐藏打印表单按钮
    }
</script>
```

## 隱藏預覽按鈕 ​

![showPreviewBtn](https://pro.form-create.com/doc/img/hidden3.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showPreviewBtn: false // 隐藏预览按钮
    }
</script>
```

## 隱藏拖拽操作按鈕 ​

控制是否顯示拖拽操作的按鈕。設定為true會隱藏這些按鈕。

![hiddenDragMenu](https://pro.form-create.com/doc/img/hiddenDragMenu.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        hiddenDragMenu: true // 隐藏拖拽操作按钮
    }
</script>
```

## 隱藏拖拽按鈕 ​

控制是否顯示拖拽按鈕。設定為true會隱藏這些按鈕。

![hiddenDragBtn](https://pro.form-create.com/doc/img/hiddenDragBtn.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        hiddenDragBtn: true // 隐藏拖拽按钮
    }
</script>
```

## 顯示儲存按鈕 ​

控制是否顯示儲存按鈕。設定為true會顯示儲存按鈕，false則隱藏。

![showSaveBtn](https://pro.form-create.com/doc/img/showSaveBtn.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showSaveBtn: true // 显示保存按钮
    }
</script>
```

## 隱藏標籤配置 ​

![labelConfig](https://pro.form-create.com/doc/img/labelConfig.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        labelConfig: false // 隐藏标签配置
    }
</script>
```

## 隱藏表單配置 ​

控制是否顯示錶單配置介面。設定為false會隱藏表單配置。

![showFormConfig](https://pro.form-create.com/doc/img/showFormConfig.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showFormConfig: false // 隐藏表单配置
    }
</script>
```

## 隱藏錄入預設資料按鈕 ​

控制是否顯示錄入預設資料按鈕。設定為false會隱藏錄入預設資料的按鈕。

![showInputData](https://pro.form-create.com/doc/img/showInputData.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showInputData: false // 隐藏录入默认数据按钮
    }
</script>
```

## 隱藏左側的模板列表 ​

控制是否顯示左側的模板列表。設定為false會隱藏模板列表。

![showTemplate](https://pro.form-create.com/doc/img/showTemplate.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showTemplate: false // 隐藏左侧模板列表
    }
</script>
```

## 隱藏左側的多語言配置 ​

控制是否顯示左側的多語言配置。設定為false會隱藏多語言配置。

![showTemplate](https://pro.form-create.com/doc/img/version-550-2.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showLanguage: false // 隐藏多语言配置
    }
</script>
```

## 隱藏左側的JSON預覽 ​

控制是否顯示左側的JSON預覽模組。設定為false會隱藏JSON預覽模組。

![image](https://pro.form-create.com/doc/img/version-541-2.png)vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showJsonPreview: false // 隐藏JSON预览模块
    }
</script>
```

## 隱藏右側的配置介面 ​

控制是否顯示右側的配置介面。設定為false會隱藏配置介面。

![showConfig](https://pro.form-create.com/doc/img/showConfig.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showConfig: false // 隐藏右侧配置界面
    }
</script>
```

## 隱藏多端適配選項 ​

控制是否顯示多端適配選項。設定為false會隱藏這些選項。

![showDevice](https://pro.form-create.com/doc/img/showDevice.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showDevice: false // 隐藏多端适配选项
    }
</script>
```

## 隱藏模組管理 ​

控制是否顯示模組管理功能。設定為false會隱藏模組管理。

![showPageManage](https://pro.form-create.com/doc/img/showPageManage.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showPageManage: false // 隐藏模块管理
    }
</script>
```

## 隱藏元件編碼輸入框 ​

![showComponentName](https://pro.form-create.com/doc/img/showComponentName.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showComponentName: false // 隐藏标签配置
    }
</script>
```

## 隱藏元件的基礎配置表單 ​

控制是否顯示元件的基礎配置表單。設定為false會隱藏基礎配置表單。

![showBaseForm](https://pro.form-create.com/doc/img/showBaseForm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showBaseForm: false // 隐藏组件的基础配置表单
    }
</script>
```

## 隱藏元件的聯動資料配置 ​

控制是否顯示元件的聯動資料配置。設定為false會隱藏聯動資料配置。

![showControl](https://pro.form-create.com/doc/img/showControl.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showControl: false // 隐藏组件的联动数据配置
    }
</script>
```

## 隱藏元件的高階配置 ​

控制是否顯示元件的高階配置表單。設定為false會隱藏高階配置表單。

![showAdvancedForm](https://pro.form-create.com/doc/img/showAdvancedForm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showAdvancedForm: false // 隐藏组件的高级配置
    }
</script>
```

## 隱藏元件的屬性配置 ​

控制是否顯示元件的屬性配置表單。設定為false會隱藏屬性配置表單。

![showPropsForm](https://pro.form-create.com/doc/img/showPropsForm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showPropsForm: false // 隐藏组件的属性配置
    }
</script>
```

## 隱藏元件的自定義屬性配置 ​

控制是否顯示元件的自定義屬性配置。設定為false時會隱藏。

![image](https://pro.form-create.com/doc/img/version-541-1.png)vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showCustomProps: false // 隐藏组件的自定义属性配置
    }
</script>
```

## 隱藏元件的樣式配置 ​

控制是否顯示元件的樣式配置表單。設定為false會隱藏樣式配置表單。

![showStyleForm](https://pro.form-create.com/doc/img/showStyleForm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showStyleForm: false // 隐藏组件的样式配置
    }
</script>
```

## 隱藏元件的事件配置 ​

控制是否顯示元件的事件配置表單。設定為false會隱藏事件配置表單。

![showEventForm](https://pro.form-create.com/doc/img/showEventForm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
const config = {
    showEventForm: false // 隐藏组件的事件配置
}
</script>
```

## 隱藏元件的驗證配置 ​

控制是否顯示元件的驗證配置表單。設定為false會隱藏驗證配置表單。

![showValidateForm](https://pro.form-create.com/doc/img/showValidateForm.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showValidateForm: false // 隐藏组件的验证配置
    }
</script>
```

## 只顯示元件必填驗證 ​

控制是否只顯示元件必填驗證。設定為true會隱藏驗證配置表單。

![validateOnlyRequired](https://pro.form-create.com/doc/img/validateOnlyRequired.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        validateOnlyRequired: true // 只显示组件必填验证
    }
</script>
```

## 隱藏匯入Excel按鈕 ​

控制是否顯示匯入Excel按鈕。設定為false會隱藏匯入Excel按鈕。

![showImportBtn](https://pro.form-create.com/doc/img/showImportBtn.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showImportBtn: false // 隐藏导入Excel按钮
    }
</script>
```

## 隱藏繫結變數 ​

控制是否顯示繫結變數功能。設定為false會隱藏繫結變數相關功能。

![showVariable](https://pro.form-create.com/doc/img/showVariable.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showVariable: false // 隐藏绑定变量
    }
</script>
```

## 隱藏標籤編輯 ​

控制是否顯示標籤編輯功能。設定為false會隱藏標籤編輯相關功能。

![showLabelConfig](https://pro.form-create.com/doc/img/showLabelConfig.png)

vue

```
<template>
    <fc-designer ref="designer" :config="config"/>
</template>
<script setup>
    const config = {
        showLabelConfig: false // 隐藏标签编辑
    }
</script>
```