# mcp-library docker-compose(疊加)用的密鑰 — 複製成 .env
# 各 MCP 獨立 token;留空則該服務公開存取。

# fc-designer-mcp (port 5689)
FC_MCP_AUTH_TOKEN=

# sqlsugar-mcp (port 5688) — 對應 sqlsugar compose 的 ${MCP_AUTH_TOKEN}
MCP_AUTH_TOKEN=

# 日後 einvoice (port 5690):
# EINVOICE_MCP_AUTH_TOKEN=
