[//]: # (sqlsugar-source)
> 📖 官方文件:[果糖網 SqlSugar 5x 文件](https://www.donet5.com/Home/Doc?typeId=1218)


[//]: # (sqlsugar-cheatsheet)
## 速查表

| 需求 | 語法 |
|---|---|
| 連接字串 | server=.;uid=sa;pwd=..;database=..;Encrypt=True;TrustServerCertificate=True |
| .NET8 全球化錯誤 | csproj <InvariantGlobalization>false</InvariantGlobalization> |
| NoLock(髒讀) | .With(SqlWith.NoLock) |
| 全域 NoLock | MoreSettings.IsWithNoLockQuery=true |
| varchar 索引優化 | [SugarColumn(SqlParameterDbType=DbType.AnsiString)] / DisableNvarchar |
| 指定索引 | .With("WITH(INDEX(idx))") |
| Schema | ConfigureExternalServices.EntityNameService 設 schema.表 |
| 連不上庫 | 裝 TLS1.2 補丁 / TrustServerCertificate=true |
| geometry | [SugarColumn(ColumnDataType="geometry")] |

---

# **.NET 操作 SqlServer**

## **連接字符串**

注意1：部分版本要加上 Encrypt=True;TrustServerCertificate=True;

注意2: .net8出現全球化錯誤看下面標題 【.NET8配置注意】

```
//SqlSugar或者SqlSugarCore 1.1.4.68以下版本
server=.;uid=sa;pwd=haosql;database=SQLSUGAR4XTEST

//SqlSugarCore 5.1.4.169上以上版本
server=.;uid=sa;pwd=haosql;database=SQLSUGAR4XTEST;Encrypt=True;TrustServerCertificate=True;
```

如果ORM連接不上數據庫：
請使用原生寫法測試：new SqlConnection("字符串").Open(); 原生寫法通過ORM就能用

```
new SqlConnection(db.CurrentConfig.ConnectionString).Open()//原生進行測試 是否是 SqlSUgar問題
```

如果是偶發情況：那麼就使用SqlSugarScope 單例模式保證線程安全或者排查線程問題

注意：連不上庫看一下標題10

## **.NET8配置注意**

SqlServer 改一下啟動項目的 csproj文件 ，設置為false

```
<InvariantGlobalization>false</InvariantGlobalization>
```

## **SqlServer新人注意**

SqlServer默認事務是禁止髒讀的，安全級別比較高

中大型項目DBA規範: 事務外的查詢必須加NoLock

我參與過歐美大型項目和一些微軟戰略合作夥伴項目都是這個規範

## **1、With( nolock ) 語法**

### **1.1 nolock**

允許髒讀 nolcok nolcok nolcok nolcok

```
//單個設置 無鎖
db.Queryable<Order>().With(SqlWith.NoLock).ToList()

//全局設置
var db= new SqlSugarClient(new ConnectionConfig()
{
DbType = SqlSugar.DbType.SqlServer,
ConnectionString = Config.ConnectionString,
IsAutoCloseConnection = true,
MoreSettings=new ConnMoreSettings() {
IsWithNoLockQuery=true//看這裡
//新功能：事務禁用NoLock 
//DisableWithNoLockWithTran = true
//需要和IsWithNoLockQuery一起使用才有效果
});

//全局設置禁用自動Nolock，只針對當前查詢禁用
db.Queryable<Order>().With(SqlWith.Null).ToList()

//全局設置禁用自動Nolock,只針對當前上下文禁用
db.CurrentConnectionConfig.MoreSettings.IsWithNoLockQuery = false;
```

### **1.2 With Index**

```
db.Queryable<Order>().With("with(indexname, NoLock)").ToList()
```

## **2、Linux Centeros**

建議用高版本的SqlServer數據庫

## **3、常見性能優化**

### **3.1 N''參數性能優化**

原理：ORM默認字符串都是nvarchar參數的方式傳到數據庫

問題：特殊情況會影響索引，導致性能失效，並不是所有SQL都有影響

方案：

```
//1通過特性指定類型
[SugarColumn(SqlParameterDbType =System.Data.DbType.AnsiString)] 
public string  name { get; set; }

//2. 全局操作 （適合所有表都沒有nvarchar）
DbType = SqlSugar.DbType.Oracle,
ConnectionString = Config.ConnectionString,
InitKeyType = InitKeyType.Attribute,
MoreSettings=new ConnMoreSettings() {
DisableNvarchar=true//添加這一行 ,將參數全部轉成varchar模式
}

//3. 指定當方法 (在當前方法加上這行，這個方法裡面會生效)
db.CurrentConnectionConfig.MoreSettings=new MoreSettings(){DisableNvarchar=true};

//4. 指定具體代碼   （具體的條件會生效）
Where(it=>it.Name==SqlFunc.ToVarchar("張"))//函數只能用到參數上面，字段要走索引禁止用函數

//5. 原生SQL用法
var name=new SugarParameter("@name","haha",System.Data.DbType.AnsiString)
```

### **3.2 NoLock性能優化**

sqlserver查詢默認是不能髒讀的，所以在非事務的查詢操作一定開啟nolock，行業規範，一般寫sqlserver都這樣操作

### **3.3 異步:大文本或者大文件**

升級SqlSugarCore到5.1.4.136及以上，或者單獨安裝 Microsoft.Data.SqlClient到最新

### **3.4 異步 調試啟動 .NET API慢**

需要ctrl+F5啟動項目 或者發布項目測試，SqlServer驅動異步在調試下不快，ctrl+F5啟動項目 測試就行了

## **4、SqlServer中geometry**

實體定義

```
public class UnitGe 
{
[SugarColumn(ColumnDataType = "geometry")]
public string geometry1 { get; set; }
}
```

代碼

```
db.Insertable(new UnitGe() { geometry1 = "POINT (20 180)" }).ExecuteCommand();
var gelist=db.Queryable<UnitGe>().Select(it=>new { geometry1 = it.geometry1.ToString()}).ToList();
```

用例1

GemoSqlServer.rar

用例2

SqlSugarUdtDemo.zip

## **5、Sql 存儲過程 DataTable參數**

表變量、表值、表參數

```
//SqlSugar中用法
var s = new SugarParameter("@p", dt);
s.TypeName = "dtTableName";   

//等同於原生SQL
//sqlParameter.TypeName = "dtTableName";
//sqlParameter.SqlDbType = SqlDbType.Structured;
```

## **6、sql server 2000**

對於這版本請使用 .net framework版本SqlSugar ，如果用低版本SqlSugar InitKey=InitKey.Attribute要設置

功能除了CodeFirst 和 分頁其他功能應該沒問題

## **7、微軟連不上大全**

https://docs.microsoft.com/zh-cn/sql/connect/ado-net/sqlclient-troubleshooting-guide?view=sql-server-ver15

## **8、CodeFirst默認使用Nvarchar**

```
DbType = SqlSugar.DbType.SqlServer,
ConnectionString = Config.ConnectionString,
IsAutoCloseConnection = true,
MoreSettings=new ConnMoreSettings() {
SqlServerCodeFirstNvarchar= true,//建表字符串默認Nvarchar
}
```

## **9、指定索引**

SqlServer中指定索引

```
db.Queryable<T>.With("WITH(INDEX(index_name))").ToList();
```

## **10、Schema用法**

注意：這個sqlserver中用法，其他庫看其他庫用法

```
var db = new SqlSugarClient(new ConnectionConfig()
{ 
IsAutoCloseConnection = true,
DbType = DbType.SqlServer,
ConnectionString = Connection,
ConfigureExternalServices = new ConfigureExternalServices()
{
EntityNameService = (type, entity) =>
{
entity.DbTableName = $"myschema1.{entity.DbTableName}";
}
}
});//這個是sqlserver中用法，不同庫看具體文檔
```

## **11、連不上庫解決方案**

### **11.1 Sqlsugar linux下 連接sqlserver 2008 提示tsl/ssl握手錯誤**

安裝一個sqlserver tls1.2補丁包(SQLServer2008R2-KB4057113-x64)就解決了

### **11.2 The negotiated TLS 1.0錯誤**

The negotiated TLS 1.0 is an insecure protocol and is supported for backward compatibility only. The recommended protocol version is TLS 1.2 and later.

解決方案：

安裝一個sqlserver tls1.2補丁包 或者 升級SqlServer

### **11.3 successfully established**

Microsoft.Data.SqlClient.SqlException (0x80131904): A connection was successfully established with the server, but then an error occurred during the login process. (provider: SSL Provider, error: 0 - The certificate chain was issued by an authority that is not trusted.) ---> System.ComponentModel.Win32Exception (0x80090325): The certificate chain was issued by an authority that is not trusted.

方案1. 在客戶端環境中安裝目標 SQL Server 的 TLS/SSL 證書。 如果需要加密，將對其進行驗證。

方案2.（不太安全）在連接字符串中設置"TrustServerCertificate=true"屬性。

方案3.（不安全的解決方案）在 docker 映像/客戶端環境中配置 TLS/SSL 設置，以便與 TLS 1.0 連接。

MinProtocol = TLSv1

CipherString = DEFAULT@SECLEVEL=1

### **11.4 Only the invariant globalization**

Only the invariant culture is supported in globalization-invariant mode. See https://aka.ms/GlobalizationInvariantMode for more information. (Parameter 'name')

```
//SqlServer如果報下面配置相關的錯要改成false
<InvariantGlobalization>false</InvariantGlobalization>    
//改一下啟動文件把這個設為false
```

是啟動項目的.csproj文件

### **11.5 更多**

https://docs.microsoft.com/zh-cn/sql/connect/ado-net/sqlclient-troubleshooting-guide?view=sql-server-ver15

*内容由 AI 生成仅供参考*