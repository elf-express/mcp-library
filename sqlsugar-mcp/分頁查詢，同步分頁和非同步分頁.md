# **分頁查詢，同步分頁和非同步分頁**

[//]: # (sqlsugar-source)
> 📖 官方文件:[果糖網 SqlSugar 5x 文件](https://www.donet5.com/Home/Doc?typeId=2242)


[//]: # (sqlsugar-cheatsheet)
## 速查表

| 需求 | 語法 |
|---|---|
| 同步分頁 | db.Queryable<T>().ToPageList(pageIndex, pageSize, ref total) |
| 非同步分頁 | await ...ToPageListAsync(pageIndex, pageSize, totalRef) |
| 總數參數 | RefAsync<int> total = 0 |
| 總頁數 | RefAsync<int> totalPage |
| Vben 格式 | 回傳 { items, total } |

---

```csharp
.ToPageList(pagenumber, pageSize)// 不返回Count
.ToPageList(pagenumber, pageSize, ref totalCount)//返回Count
.ToPageList(pagenumber, pageSize, ref totalCount,ref totalPage)//返回Count+總頁數
//如果資料庫版本較新可以用 ToOffsetPage 取代 ToPageList
//ToPageList 對低版本資料庫支援更好
//注意ToPagedList有d的不是sqlsugar封裝的
```

```csharp
int pagenumber= 1; // pagenumber是從1開始的不是從零開始的
int pageSize = 20;
int totalCount=0;
//單表分頁
var page = db.Queryable<Student>().ToPageList(pagenumber, pageSize, ref totalCount);
//如果SqlServer不想有Rownumber可以用 ToOffsetPage 較新版本支援


//多表分頁
var list = db.Queryable<Student>().LeftJoin<School>((st,sc)=>st.SchoolId==sc.Id)
.Select((st,sc)=>new{Id=st.Id,Name=st.Name,SchoolName=sc.Name})
.ToPageList(pageIndex, pageSize, ref totalCount);
```

```csharp
RefAsync<int> total = 0;//REF和OUT不支援非同步,想要真的非同步這是最優解
Db.Queryable<Order>().ToPageListAsync(pagenumber, pageSize, total);//ToPageAsync
```

```sql
SELECT * FROM 
            (SELECT [ID],[SchoolId],[Name],[CreateTime],ROW_NUMBER() 
             OVER(ORDER BY GetDate())AS RowIndex FROM [STudent]) T
   WHERE RowIndex BETWEEN 1 AND 20
```

5.0.3.2支援

```csharp
把  ToPageList 換成  ToOffsetPage   //offest分頁
```

新功能 ： 5.1.2.6-preview03
Oracle分了2種分頁，特殊情況下效能慢可以換下面這種

```csharp
把  ToPageList 換成  ToOffsetPage   //offest分頁
```

方式1：只支援SqlServer或者Oracle （ row_index ）

```csharp
[SugarColumn(IsIgnore=true)]//需要加上
public int RowIndex{get;set;} //行號 序號
db.Queryable<Student>().ToPageList(pageIndex, pageSize, ref totalCount)
```

方式2：都通用

```csharp
//其他資料庫可以這麼實現
int i = 1;
var getAll = db.Queryable<Order>().Mapper((it,cache)=> {
                it.num= i;//有分頁的話需要計算一下 (pageindex-1)*pagesize+i
                i++; 
}).ToList();
```

方式3:開視窗函式 (資料庫需要支援開視窗函式才能用)

```csharp

//開視窗函式實現
//index  =  SqlFunc.RowNumber($"{it.Id} asc ,{it.Name} desc ");
```

答： 1億資料Count就要1分鐘，別說int.max的20億資料了，所以超過1000萬以上的查詢就不建議查詢count，因為count的時間比查詢資料的時間更長

[donet5.com](https://www.donet5.com/)Apache Licence 2.0

*內容由 AI 生成僅供參考*
